$(window).resize(function() {
  // This will execute whenever the window is resized
  var mywindowheight = $(window).height(); // New height
  var mywindowwidth = $(window).width(); // New width
  
  if (mywindowwidth > 640) {
 
 }

});
